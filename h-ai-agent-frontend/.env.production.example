# 复制为 .env.production 后按服务器实际情况修改，再执行 npm run build
#
# 【推荐】前后端同域名，由 Nginx 把 /api 反代到 Spring Boot（8123）
# 构建产物请求会自动变成：https://你的域名/api/...
VITE_API_BASE_URL=/api
#
# 【备选】前后端不同域名/端口（需后端 CORS 放行）
# VITE_API_BASE_URL=https://api.example.com/api
#
# 若前端部署在子路径（如 https://域名/shopmate/），还需在 vite.config.js 设置 base: '/shopmate/'
